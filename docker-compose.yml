services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: vernet
      POSTGRES_USER: vernet
      POSTGRES_PASSWORD: vernet
      TZ: Africa/Douala
    ports:
      - "5432:5432"
    volumes:
      - vernet_pgdata:/var/lib/postgresql/data

volumes:
  vernet_pgdata:

